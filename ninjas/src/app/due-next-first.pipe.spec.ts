import { DueNextFirstPipe } from './due-next-first.pipe';

describe('DueNextFirstPipe', () => {
  it('create an instance', () => {
    const pipe = new DueNextFirstPipe();
    expect(pipe).toBeTruthy();
  });
});
